# Copy to .env.deploy (gitignored -- your droplet's IP shouldn't end up in this public repo)
# and fill in real values. Used by scripts/deploy_to_droplet.ps1.

DROPLET_HOST=203.0.113.10
DROPLET_USER=root
DROPLET_PATH=/opt/myleadsmap
SSH_KEY_PATH=~/.ssh/myleadsmap_droplet
